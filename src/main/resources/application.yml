spring:
  application:
    name: portfolio-cms

  # 開発時は H2 のインメモリで動かす。本番では PostgreSQL に切り替える
  datasource:
    url: jdbc:h2:mem:portfolio;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      # スキーマは Flyway 側で管理。validate でズレを検知する
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  thymeleaf:
    cache: false  # 開発中はキャッシュOFF

server:
  port: 8080
